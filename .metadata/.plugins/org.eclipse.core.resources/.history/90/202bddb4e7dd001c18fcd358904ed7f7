package models;

public class Emprestimo {

	public static float taxa = 5.0f;
	
	public static float calcularValorFinal(float valor, int parcela) {
		float taxa;
		if ()
		
		return (valor + (valor * taxa));
	}
	
}
